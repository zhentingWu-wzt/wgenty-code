# Rust
/target/
*.pdb

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Build artifacts
*.exe
*.dll
*.so
*.dylib
.wgenty-code/traces/
# Test
/test-project/
*.json.bak

# Config (keep template)
settings.json
!settings.json.template

# Environment variables / secrets (prevent accidental commit)
.env
.env.*

# Logs
*.log

# CodeGraph index
.codegraph/
traces/

# Project-local runtime state (memory, sessions, checkpoints) - not skill definitions
.wgenty-code/memory/
.wgenty-code/sessions/
.wgenty-code/checkpoints/
.wgenty-permission-in-workspace.txt

# Worktrees
.claude/worktrees/
.worktrees/

# Internal tool configs (not part of the public project)
.claude/
.codex/
.zcode/
# Comet local session pointer (root only; openspec change .comet/ is committed)
/.comet/
# Comet transient session state under openspec changes (handoff/ + .comet.yaml kept)
openspec/**/.comet/run-state.json
openspec/**/.comet/checkpoint.json
openspec/**/.comet/context.md
openspec/**/.comet/artifacts.json
openspec/**/.comet/state-events.jsonl
openspec/**/.comet/trajectory.jsonl
openspec/**/.comet/skill-snapshots/

# OpenSpec development workflow artifacts

# Benchmark output
benchmark_results/
.team
.tmp-target

# Install scripts (self-contained, don't ignore)
# install.ps1 and install-unix.sh are part of the repo
.wgenty-code/traces/s-nested.jsonl
