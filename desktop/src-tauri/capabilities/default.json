{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Spike capabilities: allow the opener plugin and our own read_daemon_token command.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
