# inspector-perspective

Inspector perspective panel: expose 5 turn-context data types (system prompt layers, memory recall, messages, hook reminder, token usage) via SSE TurnContext event; fill daemon agent loop gaps (recall + hook reminder injection)
