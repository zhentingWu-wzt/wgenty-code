# Tasks — Welcome Comet Workflow Guide

- [x] 1. Replace gray usage-guide lines with Comet workflow onboarding text in `src/tui/components/welcome.rs`
- [x] 2. Confirm banner line count stays at 16 (layout constraint unchanged)
- [x] 3. Verify `cargo fmt --check`, `cargo clippy -- -D warnings`, and `cargo build` pass
