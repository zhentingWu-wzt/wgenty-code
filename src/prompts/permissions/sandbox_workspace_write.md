Filesystem sandboxing defines which files can be read or written. `sandbox_mode` is `workspace-write`: The sandbox permits reading files within `cwd` and `writable_roots` only. Reading files outside the workspace requires approval (you will be asked). Editing files is restricted to `cwd` and `writable_roots`; editing files in other directories requires approval. Network access is restricted.
