# Lock registry for reproducible installs.
# Prevents per-developer ~/.npmrc (e.g. npmmirror/taobao) from rewriting the
# `resolved` URLs in package-lock.json, which produces a mixed-source lockfile
# and causes install failures when a mirror is unreachable or MITM'd.
registry=https://registry.npmjs.org/