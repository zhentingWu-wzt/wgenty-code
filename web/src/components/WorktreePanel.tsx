import type { DaemonClient } from "../api/client";

/** Placeholder for Task 10: worktree list in the left rail. */
export function WorktreePanel({ client }: { client: DaemonClient }) {
  void client; // Task 10 fetches and renders the worktree list.
  return null;
}
